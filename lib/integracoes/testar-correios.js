const {json,exigirAdmin}=require('./_utils');const {token,cfg}=require('./_correios');
module.exports=async(req,res)=>{if(req.method!=='GET')return json(res,405,{ok:false,erro:'Método não permitido.'});if(!exigirAdmin(req,res))return;try{await token();const c=cfg();return json(res,200,{ok:true,mensagem:'Autenticação Correios OK',cep_origem_configurado:c.cepOrigem.length===8,cartao_configurado:!!c.cartao,contrato_configurado:!!c.contrato,servicos:c.servicos});}catch(e){return json(res,e.httpStatus||502,{ok:false,erro:e.message,resposta:e.resposta||null});}};
