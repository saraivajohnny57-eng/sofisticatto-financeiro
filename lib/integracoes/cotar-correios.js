const {json,exigirAdmin}=require('./_utils');const {cotar}=require('./_correios');
module.exports=async(req,res)=>{if(req.method!=='POST')return json(res,405,{ok:false,erro:'Método não permitido.'});if(!exigirAdmin(req,res))return;try{const b=req.body||{};const r=await cotar({cepDestino:b.cep_destino,pesoKg:b.peso_total,comprimento:b.comprimento_cm||20,largura:b.largura_cm||20,altura:b.altura_cm||20,servicos:b.servicos});return json(res,200,{ok:true,...r});}catch(e){return json(res,e.httpStatus||502,{ok:false,erro:e.message,resposta:e.resposta||null});}};
